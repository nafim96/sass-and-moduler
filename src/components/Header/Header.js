import React from "react";
import style from "./Header.module.css";

const Header = () => {
  return (
    <div>
      <h2 className={style.title}>This is Headers</h2>
    </div>
  );
};

export default Header;
