import React from "react";
import style from "./Footer.module.css";

const Footer = () => {
  return (
    <div>
      <p>
        <small className={style.title}>This is footer</small>
      </p>
    </div>
  );
};

export default Footer;
